#include "stdio.h"
#define STACK_SIZE 40
//palindrome olup olmadığını kontrol eden fonksiyon

struct queue
{
    int front;
    int rear;
    int size;
    char *array;
};

struct stack
{
    int top;
    int data[STACK_SIZE];
};  



int main()
{

struct stack *stk = (struct stack *)malloc(sizeof(struct stack));


char 



}

if(pop(&)